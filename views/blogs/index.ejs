<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs') %>
  <body>
    <%- include('../partials/nav.ejs') %>

    
    <div class="addEntry">
      <button onclick=" window.location.href = '/blogs/create'" class="addTest">Import a new test</button></a>
    </div>
    
    <div class="blogs content">
      
      <% if(blogs.length > 0){ %> <% blogs.forEach(blog => { %>
      <a class="single" href="/blogs/<%= blog._id %>">
        <h3 class="title"><%= blog.title %></h3>
        <p class="snippet"><%= blog.snippet %></p>
      </a>
      <% }) %> <% } else { %>
      <p>No Blogs to display yet!</p>
      <% } %>
    
      <div class="info">Student view todo</div>
      <h2>Current assessments</h2>
      <h2>Completed assessments</h2>
      <p>Here the completed assessments</p>
    
    </div>

    <%- include('../partials/footer.ejs') %>
  </body>
</html>
